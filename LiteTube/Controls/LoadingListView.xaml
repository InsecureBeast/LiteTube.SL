<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:LiteTube.Converters"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit">


    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" FalseValue="Collapsed" TrueValue="Visible"/>

    <DataTemplate x:Key="LoadingListView" >
        <Grid x:Name="ContentRoot">
            <phone:LongListSelector Margin="0,0,-22,0" 
                                    ItemsSource="{Binding Items}"
                                    toolkit:TiltEffect.IsTiltEnabled="True">
                <phone:LongListSelector.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0" 
                                  HorizontalAlignment="Left"
                                  Background="DimGray"
                                  Height="100"
                                  Width="178">

                                <Image Source="{Binding ImagePath}" 
                                       Stretch="UniformToFill" />

                                <Border Margin="2"
                                        VerticalAlignment="Bottom"
                                        HorizontalAlignment="Right"
                                        Background="Black"
                                        Padding="4,0,4,0">
                                    <TextBlock TextWrapping="NoWrap" 
                                               Foreground="White"
                                               FontSize="14"/>
                                </Border>
                            </Grid>

                            <StackPanel Grid.Column="1" 
                                        Margin="0,0,0,0">
                                <TextBlock Text="{Binding Title}" 
                                           Style="{StaticResource PhoneTextNormalStyle}"
                                           TextTrimming="WordEllipsis"
                                           TextWrapping="Wrap"
                                           MaxHeight="54"/>
                                <TextBlock Text="{Binding ChannelTitle}" 
                                           Style="{StaticResource PhoneTextSmallStyle}"
                                           TextWrapping="NoWrap"/>
                                <TextBlock TextWrapping="NoWrap"
                                           Text="{Binding PublishedAt}"
                                           Style="{StaticResource PhoneTextSmallStyle}">
                                    <Run Text="Published at " x:Uid="PublishedAt"/>
                                    <Run Text="{Binding PublishedAt}" />
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                    <!--<i:Interaction.Behaviors>
                        <interactivity: LoadMoreCommand="{Binding LoadMoreCommand}"/>
                    </i:Interaction.Behaviors>-->
                </phone:LongListSelector.ItemTemplate>
            </phone:LongListSelector>
            
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       x:Uid="Loading"
                       Text="Loading..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       x:Uid="IsEmpty"
                       Text="Items not found"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </DataTemplate>
<!--
    <DataTemplate x:Key="CategoryLoadingListView" >
        <Grid>
            <ListView ItemsSource="{Binding CategoryItems}"
                      ItemTemplate="{StaticResource CategoryItemTemplate}"
                      SelectionMode="None"
                      IsSwipeEnabled="false"
                      IsItemClickEnabled="True"
                      common:ItemClickCommand.Command="{Binding ItemClickCommand}"
                      common:ItemClickCommand.CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}, Path=SelectedItem}">
                <i:Interaction.Behaviors>
                    <interactivity:LoadItemsByScrollBehavior IsEnabled="True"
                                                             LoadMoreCommand="{Binding LoadMoreCommand}"/>
                </i:Interaction.Behaviors>
            </ListView>

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       x:Uid="Loading"
                       Text="Loading..."
                       Style="{StaticResource SubheaderTextBlockStyle}"
                       Foreground="{ThemeResource ContentForegroundBrush}"
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       x:Uid="IsEmpty"
                       Text="Items not found"
                       Style="{StaticResource SubheaderTextBlockStyle}"
                       Foreground="{ThemeResource ContentForegroundBrush}"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="CommentsLoadingListView" >
        <Grid x:Name="ContentRoot">
            <controls:GenericListView ItemsSource="{Binding Comments}"
                                      SelectionMode="{Binding SelectionMode}"
                                      SelectedItems2="{Binding SelectedItems, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      IsSwipeEnabled="false"
                                      ItemContainerStyle="{StaticResource CommentListViewItemStyle}"
                                      IsItemClickEnabled="False">
                <i:Interaction.Behaviors>
                    <interactivity:LoadItemsByScrollBehavior IsEnabled="True"
                                                             LoadMoreCommand="{Binding LoadMoreCommand}"/>
                </i:Interaction.Behaviors>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="{Binding ActualWidth, ElementName=ContentRoot}" 
                              Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Image Source="{Binding AuthorProfileImageUrl}" 
                                   Stretch="UniformToFill"
                                   Height="45"
                                   Width="45"
                                   VerticalAlignment="Top"
                                   Margin="{Binding IsReplay, Converter={StaticResource CommentMarginConverter}}"/>

                            <StackPanel Grid.Column="1"
                                        Margin="8,0,0,0">

                                <StackPanel Orientation="Horizontal">
                                    <Button Content="{Binding AuthorDisplayName}"
                                            Style="{StaticResource AccentTextBlockButtonStyle}"
                                            Margin="0,-3,0,0"
                                            Click="OnCommentButtonClick"/>
                                    <TextBlock TextWrapping="NoWrap"
                                               Style="{StaticResource CommentPublishedTextBlockStyle}"
                                               Margin="10,0,0,0"
                                               Text="{Binding PublishedAtRaw}"/>
                                </StackPanel>

                                <TextBlock Text="{Binding TextDisplay}" 
                                           Style="{StaticResource CommentTextBlockStyle}" 
                                           Margin="0,0,0,0"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>

                </ListView.ItemTemplate>
            </controls:GenericListView>

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       x:Uid="Loading"
                       Text="Loading..."
                       Style="{StaticResource SubheaderTextBlockStyle}"
                       Foreground="{ThemeResource ContentForegroundBrush}"
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       x:Uid="IsEmpty"
                       Text="Items not found"
                       Style="{StaticResource SubheaderTextBlockStyle}"
                       Foreground="{ThemeResource ContentForegroundBrush}"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </DataTemplate>
    -->
</ResourceDictionary>
